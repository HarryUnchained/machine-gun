{
  "name": "@machine-gun/common",
  "version": "1.0.0",
  "description": "Shared contracts and utilities for the Machine Gun dynamic load testing tool and schema driven event simulation platform for RabbitMQ and Kafka.",
  "private": true,
  "type": "commonjs",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint \"src/**/*.ts\" --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}